
'use strict';

/*

You aren't going to understand Node if you don't understand objects. 

Simple object declaration. 

*/

var firstObject = {};				// Objects in JavaScript are more like 
                                    // arbitrary containers. They aren't
                                    // classes, per se, like in Python - 
                                    // well not yet. The notion of 
                                    // Prototype will eventually come into
                                    // play

console.log(typeof(firstObject));   // Show that what I made is an actual
                                    // object using 'typeof.'
